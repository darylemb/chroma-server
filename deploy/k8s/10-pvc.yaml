apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chroma-data
  namespace: chroma
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 5Gi
  # storageClassName: local-path   # k3s default
